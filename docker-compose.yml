services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: development
    volumes:
      - sqlite_data:/app/storage
      - bundle_cache:/usr/local/bundle

volumes:
  sqlite_data:
  bundle_cache:
